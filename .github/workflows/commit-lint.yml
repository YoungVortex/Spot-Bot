name: Commit Lint Check

on:
  pull_request:
    branches: [main, master]

permissions:
  contents: read
  pull-requests: write

jobs:
  commit-lint:
    name: Validate Commit Messages
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Validate PR commits
        run: |
          echo "=== Validating commit messages ==="
          DEFAULT_BRANCH="${{ github.event.repository.default_branch }}"
          COMMITS=$(git log "origin/${DEFAULT_BRANCH}..HEAD" --pretty=format:"%H" 2>/dev/null || git log --pretty=format:"%H" -5)

          if [ -z "$COMMITS" ]; then
            echo "No commits to validate."
            exit 0
          fi

          FAILED=0
          for SHA in $COMMITS; do
            MSG=$(git log -1 --pretty=format:"%s" "$SHA")

            # Check conventional commit format
            if echo "$MSG" | grep -qE "^(feat|fix|docs|style|refactor|perf|test|build|ci|chore|revert)(\(.+\))?(!)?: .+"; then
              echo "OK: $MSG"
            else
              echo "FAIL: $MSG"
              echo "  Expected: type(scope): description"
              echo "  Types: feat, fix, docs, style, refactor, perf, test, build, ci, chore, revert"
              FAILED=1
            fi

            # Check subject length
            SUBJECT_LEN=${#MSG}
            if [ "$SUBJECT_LEN" -gt 100 ]; then
              echo "  TOO LONG: $SUBJECT_LEN chars (max 100)"
              FAILED=1
            fi
          done

          if [ "$FAILED" -eq 1 ]; then
            echo ""
            echo "=== Commit lint failed ==="
            echo "Please use conventional commit format:"
            echo "  feat: add new feature"
            echo "  fix: resolve bug"
            echo "  docs: update documentation"
            exit 1
          fi

          echo ""
          echo "All commits follow conventional format!"
